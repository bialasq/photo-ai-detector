# [3.2.1] Unit tests Python: 80%+ coverage na database/main/ai_core

> **Owner:** Luq | **Hours:** 18 | **Priority:** P0 | **Phase:** 3 | **Source:** PDF

## Context

Dopełnienie testów do 80% (testy z Faz 1-2 piszemy NA BIEŻĄCO — tu finishing). Luq pisze MANUALNIE — testy to ground truth, nie generujemy ich tym samym modelem co kod.

## Files affected
- `tests/`

## Dependencies
_None_

## Acceptance Criteria

- [ ] pytest --cov database.py ≥ 80%
- [ ] pytest --cov main.py ≥ 70%
- [ ] pytest --cov ai_core.py ≥ 80%
- [ ] Coverage report uploaded jako artifact
- [ ] Branch coverage (nie tylko line) tracked

## Cursor Prompt

> Wklej 1:1 do Cursor Composer / Agent mode. Pamiętaj że `.cursorrules` w roocie repo jest aktywne.

```
ROLA: Senior QA engineer.

TASK: Dopełnienie unit testów do 80%+ coverage.

UWAGA: ten task Luq robi MANUALNIE. Cursor może pomagać z scaffoldingiem,
ale testy musi czytać/zatwierdzać Luq — testy nie mogą być generowane przez ten sam
model co kod, bo tracimy ground truth.

WYMAGANIA:
1. pytest --cov=ai_core --cov=main --cov=database --cov-report html — sprawdź uncovered lines.
2. Per file:
   - database.py: każda funkcja CRUD + edge cases (empty DB, duplicate insert, NULL fields)
   - main.py: każdy endpoint × happy path + 2 error paths (validation, internal)
   - ai_core.py: detect, embed, cluster — fixtures z 1, 5, 0 twarzy
3. Fixtures: tests/fixtures/images/ (one_face.jpg, group.jpg, landscape.jpg, broken.jpg, empty.jpg)
4. Mock DeepFace w testach integracyjnych (szybko). Real model tylko w nightly.

NIE WOLNO:
- Testy które tylko 'import + assert True' (coverage cheating).
- Test name = 'test_works' (każdy test musi sprawdzać konkretne zachowanie).

ACCEPTANCE: 80%/70%/80% pokrycia + HTML report w PR.

DELIVERABLE: testy + raport coverage.
```

## Labels

`P0, phase-3, owner-luq, tests, quality`

## Definition of Done

- [ ] Kod scalony do `main` przez PR
- [ ] PR ma minimum 1 approval od drugiej osoby
- [ ] CI zielone (lint + pytest + npm build, jeśli istnieje)
- [ ] Acceptance criteria spełnione i sprawdzone manualnie
- [ ] Dokumentacja zaktualizowana jeśli zmiana publicznego API
- [ ] CHANGELOG.md ma wpis w sekcji `[Unreleased]`

---
*Generated from `docs/PRODUCTION_ROADMAP.md` task `3.2.1`.*
