# [3.2.2] Integration tests: FastAPI TestClient dla każdego endpointu

> **Owner:** Luq | **Hours:** 10 | **Priority:** P0 | **Phase:** 3 | **Source:** PDF

## Context

Każdy endpoint × happy + error path. TestClient + in-memory SQLite.

## Files affected
- `tests/integration/`

## Dependencies
_None_

## Acceptance Criteria

- [ ] Każdy endpoint ma minimum 2 testy (happy + error)
- [ ] In-memory SQLite fixture (fresh per test)
- [ ] Mock vector_store i ai_core (dla szybkości)

## Cursor Prompt

> Wklej 1:1 do Cursor Composer / Agent mode. Pamiętaj że `.cursorrules` w roocie repo jest aktywne.

```
ROLA: Senior QA + FastAPI engineer.

TASK: Integration tests z TestClient.

WYMAGANIA:
1. conftest.py: fixture `client` z TestClient(app) + in-memory DB.
2. Każdy endpoint → 2+ testy (happy + error).
3. Mock: monkeypatch.setattr('backend.app.ai_core.scan_directory', mock_fn).
4. Assertions: status code + response shape (Pydantic schema match) + side effects (DB state).

ACCEPTANCE: minimum 2 testy/endpoint × ~15 endpointów = 30+ testów.

DELIVERABLE: tests/integration/ + raport.
```

## Labels

`P0, phase-3, owner-luq, tests, quality`

## Definition of Done

- [ ] Kod scalony do `main` przez PR
- [ ] PR ma minimum 1 approval od drugiej osoby
- [ ] CI zielone (lint + pytest + npm build, jeśli istnieje)
- [ ] Acceptance criteria spełnione i sprawdzone manualnie
- [ ] Dokumentacja zaktualizowana jeśli zmiana publicznego API
- [ ] CHANGELOG.md ma wpis w sekcji `[Unreleased]`

---
*Generated from `docs/PRODUCTION_ROADMAP.md` task `3.2.2`.*
