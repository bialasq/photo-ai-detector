# [3.1.2] DNS rebinding mitigation (Host header check)

> **Owner:** Luq | **Hours:** 4 | **Priority:** P1 | **Phase:** 3 | **Source:** Cursor

## Context

ZASTĘPUJE 'rate limiting' z PDF — dla loopback nie ma sensu. Ryzyko: DNS rebinding — przeglądarka resolveuje attacker.com → 127.0.0.1, request leci do naszego sidecara z Host: attacker.com. Mitigation: middleware sprawdza Host header in ('127.0.0.1:8000', 'localhost:8000').

## Files affected
- `main.py`
- `tests/integration/test_dns_rebinding.py`

## Dependencies
_None_

## Acceptance Criteria

- [ ] Middleware checks Host header — reject != 127.0.0.1 / localhost
- [ ] Tauri webview ustawia Host = 127.0.0.1 — verify
- [ ] 1 pytest: curl -H 'Host: evil.com' → 400
- [ ] 1 pytest: curl -H 'Host: 127.0.0.1:8000' → 200

## Cursor Prompt

> Wklej 1:1 do Cursor Composer / Agent mode. Pamiętaj że `.cursorrules` w roocie repo jest aktywne.

```
ROLA: Senior security engineer.

TASK: DNS rebinding mitigation.

WYMAGANIA:
1. main.py middleware:
   ALLOWED_HOSTS = {'127.0.0.1:8000', 'localhost:8000', '127.0.0.1', 'localhost'}
   @app.middleware('http')
   async def host_check(request: Request, call_next):
     host = request.headers.get('host', '').lower()
     if host not in ALLOWED_HOSTS:
       return JSONResponse({'error': 'invalid host'}, status_code=400)
     return await call_next(request)
2. 2 pytest (good/bad host).

NIE WOLNO: zaufać X-Forwarded-Host (frontend nie kontrolujemy).

ACCEPTANCE: 2 testy + manual curl.

DELIVERABLE: diff + 2 testy.
```

## Labels

`P1, phase-3, owner-luq, backend, security`

## Definition of Done

- [ ] Kod scalony do `main` przez PR
- [ ] PR ma minimum 1 approval od drugiej osoby
- [ ] CI zielone (lint + pytest + npm build, jeśli istnieje)
- [ ] Acceptance criteria spełnione i sprawdzone manualnie
- [ ] Dokumentacja zaktualizowana jeśli zmiana publicznego API
- [ ] CHANGELOG.md ma wpis w sekcji `[Unreleased]`

---
*Generated from `docs/PRODUCTION_ROADMAP.md` task `3.1.2`.*
