# [3.2.5] GitHub Actions ci.yml (pytest + npm build + lint)

> **Owner:** Grzesiek | **Hours:** 7 | **Priority:** P0 | **Phase:** 3 | **Source:** PDF

## Context

CI obowiązkowy. Każdy PR → pytest + npm build + clippy. Branch protection rule wymaga green. KRYTYCZNE dla quality gate.

## Files affected
- `.github/workflows/ci.yml`
- `.github/workflows/release.yml`
- `docs/CI_SETUP.md`

## Dependencies
_None_

## Acceptance Criteria

- [ ] ci.yml: 3 joby (backend pytest, frontend build/test, rust clippy)
- [ ] Czas total < 5 min
- [ ] Cache pip + npm
- [ ] concurrency: cancel-in-progress
- [ ] Branch protection: Luq konfiguruje w GitHub settings (CI green required)

## Cursor Prompt

> Wklej 1:1 do Cursor Composer / Agent mode. Pamiętaj że `.cursorrules` w roocie repo jest aktywne.

```
ROLA: Senior DevOps / CI engineer.

TASK: ci.yml + release.yml.

WYMAGANIA:
1. .github/workflows/ci.yml na push/PR do main:
   - Job 'backend' (ubuntu-latest, Python 3.12):
     * actions/setup-python + cache pip
     * pip install -r requirements.txt -r requirements-dev.txt
     * ruff check main.py ai_core.py database.py (warn-only początkowo)
     * pytest --cov=ai_core --cov=main --cov=database --cov-fail-under=70
     * upload coverage.xml
   - Job 'frontend' (ubuntu-latest, Node 20):
     * actions/setup-node + cache npm
     * npm ci && npm run lint && npm run build && npm test
   - Job 'rust' (windows-latest):
     * actions-rs/toolchain stable
     * cargo clippy --manifest-path src-tauri/Cargo.toml -- -D warnings
   - concurrency: group: ci-${{github.ref}}, cancel-in-progress: true
2. .github/workflows/release.yml na tag v*:
   - npm run sidecar:package
   - npm run tauri:build
   - upload-artifact MSI (signing w 4.1.4)
3. docs/CI_SETUP.md: jak Luq ustawi branch protection (settings → branches → main → require status checks: backend, frontend, rust).

NIE WOLNO:
- Hardcode secrets.
- Full Tauri build per PR (slow ~15min) — only clippy.

ACCEPTANCE: test PR green, < 5 min.

DELIVERABLE: 2 workflow + docs.
```

## Labels

`P0, phase-3, owner-grzesiek, devops, quality, blocker`

## Definition of Done

- [ ] Kod scalony do `main` przez PR
- [ ] PR ma minimum 1 approval od drugiej osoby
- [ ] CI zielone (lint + pytest + npm build, jeśli istnieje)
- [ ] Acceptance criteria spełnione i sprawdzone manualnie
- [ ] Dokumentacja zaktualizowana jeśli zmiana publicznego API
- [ ] CHANGELOG.md ma wpis w sekcji `[Unreleased]`

---
*Generated from `docs/PRODUCTION_ROADMAP.md` task `3.2.5`.*
