# [3.1.5] pip-audit + npm audit w CI (warning only)

> **Owner:** Grzesiek | **Hours:** 4 | **Priority:** P1 | **Phase:** 3 | **Source:** PDF

## Context

Dependency scan na każdy PR. Warning (nie failing) na razie — bo TF/DeepFace mają historycznie sporo CVE i pełen fix to dłuższa praca.

## Files affected
- `.github/workflows/security_scan.yml`

## Dependencies
- 3.2.5

## Acceptance Criteria

- [ ] ci-security job: pip-audit + npm audit
- [ ] Wyniki jako artifact + komentarz na PR
- [ ] Nie failuje PR (warning only); High/Critical → kreuje issue P0

## Cursor Prompt

> Wklej 1:1 do Cursor Composer / Agent mode. Pamiętaj że `.cursorrules` w roocie repo jest aktywne.

```
ROLA: Senior DevOps + security engineer.

TASK: Dependency scan w CI.

WYMAGANIA:
1. .github/workflows/security_scan.yml — nightly + on PR:
   - pip install pip-audit
   - pip-audit -r requirements.txt --format json -o pip-audit.json (continue-on-error: true)
   - cd frontend && npm audit --json > npm-audit.json (continue-on-error: true)
   - parse json, jeśli High/Critical → gh issue create + comment on PR
2. Artifact upload obu json.
3. Dokumentuj exceptions w .github/security-exceptions.md (kontrolowany whitelist).

NIE WOLNO: failować PR na vulnerable deps (bo TF historia) — TYLKO warning.

ACCEPTANCE: 1 PR z artifactami + warning comment widoczny.

DELIVERABLE: workflow + exceptions.md.
```

## Labels

`P1, phase-3, owner-grzesiek, devops, security`

## Definition of Done

- [ ] Kod scalony do `main` przez PR
- [ ] PR ma minimum 1 approval od drugiej osoby
- [ ] CI zielone (lint + pytest + npm build, jeśli istnieje)
- [ ] Acceptance criteria spełnione i sprawdzone manualnie
- [ ] Dokumentacja zaktualizowana jeśli zmiana publicznego API
- [ ] CHANGELOG.md ma wpis w sekcji `[Unreleased]`

---
*Generated from `docs/PRODUCTION_ROADMAP.md` task `3.1.5`.*
