# [3.2.4] Benchmarks jako pytest-benchmark, fail na regresji >20%

> **Owner:** Grzesiek | **Hours:** 6 | **Priority:** P1 | **Phase:** 3 | **Source:** PDF

## Context

Z 2.0.1 mamy benchmark.py. Tu — integracja w pytest-benchmark + CI nightly + fail na regresji.

## Files affected
- `tests/perf/`
- `.github/workflows/benchmark.yml`

## Dependencies
- 2.0.1
- 3.2.5

## Acceptance Criteria

- [ ] pytest-benchmark w requirements-dev.txt
- [ ] tests/perf/test_scan_performance.py z @pytest.mark.benchmark
- [ ] Nightly CI job: run + compare with baseline + fail if regression > 20%
- [ ] Baseline stored as artifact / comparing across commits

## Cursor Prompt

> Wklej 1:1 do Cursor Composer / Agent mode. Pamiętaj że `.cursorrules` w roocie repo jest aktywne.

```
ROLA: Senior performance + DevOps engineer.

TASK: pytest-benchmark w CI z regression detection.

WYMAGANIA:
1. requirements-dev.txt: pytest-benchmark.
2. tests/perf/test_scan_performance.py:
   @pytest.mark.benchmark(group='scan')
   def test_scan_1k_photos(benchmark, scan_fixture_1k):
     result = benchmark(scan_directory, scan_fixture_1k)
     assert result.processed == 1000
3. .github/workflows/benchmark.yml — nightly:
   - pytest --benchmark-only --benchmark-json=current.json
   - download artifact baseline.json from latest main
   - pytest-benchmark compare baseline.json current.json --fail-on-regression --threshold=1.2
   - upload current.json as new baseline

ACCEPTANCE: 1 nightly run zielony + sztuczna regresja test (10% slower) → fail.

DELIVERABLE: diff + workflow + test.
```

## Labels

`P1, phase-3, owner-grzesiek, tests, performance, devops`

## Definition of Done

- [ ] Kod scalony do `main` przez PR
- [ ] PR ma minimum 1 approval od drugiej osoby
- [ ] CI zielone (lint + pytest + npm build, jeśli istnieje)
- [ ] Acceptance criteria spełnione i sprawdzone manualnie
- [ ] Dokumentacja zaktualizowana jeśli zmiana publicznego API
- [ ] CHANGELOG.md ma wpis w sekcji `[Unreleased]`

---
*Generated from `docs/PRODUCTION_ROADMAP.md` task `3.2.4`.*
