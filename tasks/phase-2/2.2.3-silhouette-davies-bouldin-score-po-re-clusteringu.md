# [2.2.3] Silhouette / Davies-Bouldin score po re-clusteringu

> **Owner:** Luq | **Hours:** 6 | **Priority:** P1 | **Phase:** 2 | **Source:** PDF

## Context

Po DBSCAN — policz quality score. Logowanie + UI 'cluster health'. Sygnał dla auto-tune (2.2.4).

## Files affected
- `ai_core.py`
- `main.py`
- `tests/test_cluster_metrics.py`

## Dependencies
_None_

## Acceptance Criteria

- [ ] Po DBSCAN: silhouette_score + davies_bouldin_score (sklearn.metrics)
- [ ] Log INFO: {phase: 'clustering', silhouette: 0.42, db_score: 1.23, n_clusters: 27}
- [ ] GET /api/v1/clusters/health → ostatni score + recommendation
- [ ] Cel: silhouette ≥ 0.4 dla biblioteki 1000+ twarzy
- [ ] 1 pytest: znana fixture → znany score

## Cursor Prompt

> Wklej 1:1 do Cursor Composer / Agent mode. Pamiętaj że `.cursorrules` w roocie repo jest aktywne.

```
ROLA: Senior ML engineer.

TASK: Cluster quality metrics.

WYMAGANIA:
1. ai_core.py after DBSCAN:
   from sklearn.metrics import silhouette_score, davies_bouldin_score
   silh = silhouette_score(embeddings, labels) if len(set(labels)) > 1 else None
   db = davies_bouldin_score(embeddings, labels) if len(set(labels)) > 1 else None
   logger.info('clustering done', extra={'silhouette': silh, 'db_score': db, 'n_clusters': len(set(labels)) - (1 if -1 in labels else 0)})
2. Persist score w cluster_health table: (id, run_at, silhouette, db_score, n_clusters, eps).
3. GET /api/v1/clusters/health → ostatni wiersz + threshold check (silh < 0.3 → warning).

ACCEPTANCE: 1 pytest na fixture + log inspection.

DELIVERABLE: diff + migracja DB + test.
```

## Labels

`P1, phase-2, owner-luq, backend, ml`

## Definition of Done

- [ ] Kod scalony do `main` przez PR
- [ ] PR ma minimum 1 approval od drugiej osoby
- [ ] CI zielone (lint + pytest + npm build, jeśli istnieje)
- [ ] Acceptance criteria spełnione i sprawdzone manualnie
- [ ] Dokumentacja zaktualizowana jeśli zmiana publicznego API
- [ ] CHANGELOG.md ma wpis w sekcji `[Unreleased]`

---
*Generated from `docs/PRODUCTION_ROADMAP.md` task `2.2.3`.*
