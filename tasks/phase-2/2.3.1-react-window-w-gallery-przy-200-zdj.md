# [2.3.1] react-window w Gallery przy >200 zdjęć

> **Owner:** Luq | **Hours:** 8 | **Priority:** P0 | **Phase:** 2 | **Source:** PDF+Cursor

## Context

Aktualnie Gallery renderuje wszystkie zdjęcia w DOM. Przy 10k → lag. react-window FixedSizeGrid → renderuje tylko visible.

## Files affected
- `src/components/Gallery.tsx`
- `src/test/Gallery.test.tsx`
- `package.json`

## Dependencies
_None_

## Acceptance Criteria

- [ ] react-window dependency added
- [ ] FixedSizeGrid lub VariableSizeGrid w Gallery
- [ ] Threshold: jeśli photos.length > 200 → virtualize
- [ ] Manual: 10k photos → scroll 60 fps p95
- [ ] Vitest: render 1000 items → assert only ~50 in DOM

## Cursor Prompt

> Wklej 1:1 do Cursor Composer / Agent mode. Pamiętaj że `.cursorrules` w roocie repo jest aktywne.

```
ROLA: Senior React engineer (performance).

TASK: react-window wirtualizacja Gallery.

WYMAGANIA:
1. npm i react-window @types/react-window.
2. Gallery.tsx:
   import { FixedSizeGrid } from 'react-window';
   const columnCount = Math.floor(width / CELL_SIZE);
   const rowCount = Math.ceil(photos.length / columnCount);
   <FixedSizeGrid columnCount={columnCount} columnWidth={CELL_SIZE} rowCount={rowCount} rowHeight={CELL_SIZE} width={width} height={height}>
     {Cell}
   </FixedSizeGrid>
3. Threshold: virtualize tylko gdy photos.length > 200 (małe galerie nie potrzebują overhead).
4. Vitest: render 1000 → query.getAllByRole('img').length < 100.

NIE WOLNO:
- Brak ResizeObserver dla columnCount (window resize → bug).

ACCEPTANCE: vitest + manual 10k zdjęć (perf monitor).

DELIVERABLE: diff + vitest + Lighthouse score before/after.
```

## Labels

`P0, phase-2, owner-luq, frontend, performance`

## Definition of Done

- [ ] Kod scalony do `main` przez PR
- [ ] PR ma minimum 1 approval od drugiej osoby
- [ ] CI zielone (lint + pytest + npm build, jeśli istnieje)
- [ ] Acceptance criteria spełnione i sprawdzone manualnie
- [ ] Dokumentacja zaktualizowana jeśli zmiana publicznego API
- [ ] CHANGELOG.md ma wpis w sekcji `[Unreleased]`

---
*Generated from `docs/PRODUCTION_ROADMAP.md` task `2.3.1`.*
