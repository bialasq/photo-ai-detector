# [2.2.4] Auto-tune eps, min_samples (grid search)

> **Owner:** Grzesiek | **Hours:** 7 | **Priority:** P2 | **Phase:** 2 | **Source:** PDF

## Context

Cron-style raz w tygodniu (lub manual button): grid search nad eps/min_samples na sample 10% embeddings → wybór best silhouette. v1.0.0: P2.

## Files affected
- `ai_core.py`
- `main.py`

## Dependencies
- 2.2.3

## Acceptance Criteria

- [ ] auto_tune_clustering() → grid search eps in [0.3, 0.4, 0.5, 0.6], min_samples in [3, 5, 8]
- [ ] Sample 10% lub max 1000 embeddings (czas)
- [ ] Wybór: max silhouette + min davies_bouldin
- [ ] POST /api/v1/clusters/auto-tune (manual trigger)
- [ ] Wynik persisted: tabela clustering_config(eps, min_samples, score, applied_at)

## Cursor Prompt

> Wklej 1:1 do Cursor Composer / Agent mode. Pamiętaj że `.cursorrules` w roocie repo jest aktywne.

```
ROLA: Senior ML engineer.

TASK: Auto-tune DBSCAN params.

WYMAGANIA:
1. ai_core.py:
   def auto_tune_clustering(sample_size: int = 1000) -> ClusteringConfig:
     embeddings = sample_embeddings_from_db(sample_size)
     best = None
     for eps in [0.3, 0.4, 0.5, 0.6]:
       for min_samples in [3, 5, 8]:
         labels = DBSCAN(eps=eps, min_samples=min_samples, metric='cosine').fit_predict(embeddings)
         if len(set(labels)) < 2: continue
         silh = silhouette_score(embeddings, labels)
         if best is None or silh > best.score:
           best = ClusteringConfig(eps, min_samples, silh)
     return best
2. POST /api/v1/clusters/auto-tune — uruchomienie + persist + apply.

ACCEPTANCE: 1 pytest na fixture + manual trigger przez UI.

DELIVERABLE: diff + test.
```

## Labels

`P2, phase-2, owner-grzesiek, backend, ml`

## Definition of Done

- [ ] Kod scalony do `main` przez PR
- [ ] PR ma minimum 1 approval od drugiej osoby
- [ ] CI zielone (lint + pytest + npm build, jeśli istnieje)
- [ ] Acceptance criteria spełnione i sprawdzone manualnie
- [ ] Dokumentacja zaktualizowana jeśli zmiana publicznego API
- [ ] CHANGELOG.md ma wpis w sekcji `[Unreleased]`

---
*Generated from `docs/PRODUCTION_ROADMAP.md` task `2.2.4`.*
