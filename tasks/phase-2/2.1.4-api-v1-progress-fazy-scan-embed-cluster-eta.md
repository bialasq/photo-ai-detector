# [2.1.4] /api/v1/progress — fazy (scan/embed/cluster) + ETA

> **Owner:** Grzesiek | **Hours:** 7 | **Priority:** P1 | **Phase:** 2 | **Source:** PDF

## Context

Aktualne progress to total + processed. Userzy chcą wiedzieć: 'jestem w fazie X / Y, ETA Z'.

## Files affected
- `main.py`
- `ai_core.py`
- `src/components/ScanProgress.tsx`

## Dependencies
- 1.4.1
- 1.4.3

## Acceptance Criteria

- [ ] Response: {phase: 'scanning'|'embedding'|'clustering'|'idle', current: int, total: int, eta_seconds: float, throughput_per_sec: float}
- [ ] ETA = (total - current) / throughput_recent (last 30s window)
- [ ] Frontend pokazuje fazę + progress bar + ETA

## Cursor Prompt

> Wklej 1:1 do Cursor Composer / Agent mode. Pamiętaj że `.cursorrules` w roocie repo jest aktywne.

```
ROLA: Senior backend + UX engineer.

TASK: Granular progress endpoint.

WYMAGANIA:
1. ai_core.py — ScanProgressState:
   - phase: Literal['idle', 'scanning', 'embedding', 'clustering', 'finalizing', 'cancelled', 'error']
   - throughput_window: deque[(timestamp, count)] — ostatnie 30s
   - throughput_per_sec property
   - eta_seconds property: (total - current) / throughput_per_sec (None if throughput=0)
2. main.py GET /api/v1/progress — JSON response.
3. Frontend ScanProgress.tsx:
   - Phase badge ('Scanning images...', 'Computing embeddings...', 'Clustering faces...')
   - Progress bar (current / total)
   - ETA: format 'About 2 minutes remaining' (humanize)

NIE WOLNO:
- Stateful counter rolling forever (window 30s rolling).
- ETA na podstawie total throughput (przyspieszanie/zwalnianie zostaje niezauważone).

ACCEPTANCE: pytest + vitest + manual skan 1k zdjęć — fazy widoczne.

DELIVERABLE: diff + screencast (opt).
```

## Labels

`P1, phase-2, owner-grzesiek, backend, frontend, ux`

## Definition of Done

- [ ] Kod scalony do `main` przez PR
- [ ] PR ma minimum 1 approval od drugiej osoby
- [ ] CI zielone (lint + pytest + npm build, jeśli istnieje)
- [ ] Acceptance criteria spełnione i sprawdzone manualnie
- [ ] Dokumentacja zaktualizowana jeśli zmiana publicznego API
- [ ] CHANGELOG.md ma wpis w sekcji `[Unreleased]`

---
*Generated from `docs/PRODUCTION_ROADMAP.md` task `2.1.4`.*
