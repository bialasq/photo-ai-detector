# [2.3.3] Cache miniaturek LRU eviction (limit 500 MB w AppData)

> **Owner:** Grzesiek | **Hours:** 6 | **Priority:** P1 | **Phase:** 2 | **Source:** Cursor

## Context

Miniatury cachujemy na dysku. Bez limitu → rośnie nieograniczenie. LRU eviction nad %AppData%\com.photo.organizer\thumbnails\ przy przekroczeniu 500 MB.

## Files affected
- `thumbnail_cache.py`
- `tests/test_thumbnail_cache.py`
- `docs/THUMBNAIL_CACHE.md`

## Dependencies
_None_

## Acceptance Criteria

- [ ] ThumbnailCache.add(key, bytes) → write to disk + update DB metadata
- [ ] ThumbnailCache.evict_lru(max_size_mb=500) — kasuje najstarsze (atime) aż mieści się
- [ ] Eviction trigger: każdy add po przekroczeniu 500 MB
- [ ] DB metadata table: thumbnail_cache(key TEXT PK, path TEXT, size_bytes INT, last_access TIMESTAMP)
- [ ] Configurable: env PHOTO_ORGANIZER_THUMB_CACHE_MB
- [ ] 3 pytest: add, evict, configurable limit

## Cursor Prompt

> Wklej 1:1 do Cursor Composer / Agent mode. Pamiętaj że `.cursorrules` w roocie repo jest aktywne.

```
ROLA: Senior backend engineer.

TASK: LRU thumbnail cache w AppData z limit 500 MB.

WYMAGANIA:
1. thumbnail_cache.py:
   class ThumbnailCache:
     def __init__(self, cache_dir: Path, max_size_mb: int = 500):
       self.cache_dir = cache_dir; cache_dir.mkdir(parents=True, exist_ok=True)
       self.max_bytes = max_size_mb * 1024 * 1024
     
     def add(self, key: str, data: bytes):
       path = self.cache_dir / f'{key}.jpg'
       path.write_bytes(data)
       db_upsert_thumbnail(key, path, len(data), now())
       if self.total_size() > self.max_bytes: self.evict_lru()
     
     def get(self, key: str) -> Optional[bytes]:
       row = db_get_thumbnail(key)
       if row is None: return None
       db_update_access(key, now())
       return Path(row.path).read_bytes()
     
     def evict_lru(self):
       # SELECT key, path, size FROM thumbnail_cache ORDER BY last_access ASC
       # delete until total_size < 0.9 * max_bytes (10% headroom)
2. Configurable: env PHOTO_ORGANIZER_THUMB_CACHE_MB (default 500).
3. Schema migration: 004_thumbnail_cache.sql (table + index na last_access).

ACCEPTANCE: 3 pytest + manual fill cache + verify eviction.

DELIVERABLE: thumbnail_cache.py + migration + 3 testy + docs.
```

## Labels

`P1, phase-2, owner-grzesiek, backend, performance`

## Definition of Done

- [ ] Kod scalony do `main` przez PR
- [ ] PR ma minimum 1 approval od drugiej osoby
- [ ] CI zielone (lint + pytest + npm build, jeśli istnieje)
- [ ] Acceptance criteria spełnione i sprawdzone manualnie
- [ ] Dokumentacja zaktualizowana jeśli zmiana publicznego API
- [ ] CHANGELOG.md ma wpis w sekcji `[Unreleased]`

---
*Generated from `docs/PRODUCTION_ROADMAP.md` task `2.3.3`.*
