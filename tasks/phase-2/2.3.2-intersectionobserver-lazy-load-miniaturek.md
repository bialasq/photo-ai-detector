# [2.3.2] IntersectionObserver lazy load miniaturek

> **Owner:** Luq | **Hours:** 6 | **Priority:** P0 | **Phase:** 2 | **Source:** PDF

## Context

Z 2.3.1 mamy wirtualizację. Tu lazy load: src miniatury ustawiany dopiero gdy element w viewport (IntersectionObserver).

## Files affected
- `src/components/LazyThumbnail.tsx`
- `src/components/Gallery.tsx`
- `src/test/LazyThumbnail.test.tsx`

## Dependencies
- 2.3.1

## Acceptance Criteria

- [ ] LazyThumbnail component: placeholder przed viewport, src po enter viewport
- [ ] IntersectionObserver z rootMargin '200px' (preload przed scrollem)
- [ ] Vitest: mock IntersectionObserver, assert lazy behavior
- [ ] Manual: Network tab w devtools — miniatury fetchowane podczas scroll

## Cursor Prompt

> Wklej 1:1 do Cursor Composer / Agent mode. Pamiętaj że `.cursorrules` w roocie repo jest aktywne.

```
ROLA: Senior React engineer (performance).

TASK: IntersectionObserver lazy loading.

WYMAGANIA:
1. src/components/LazyThumbnail.tsx:
   const LazyThumbnail = ({ src, alt }: Props) => {
     const ref = useRef<HTMLImageElement>(null);
     const [visible, setVisible] = useState(false);
     useEffect(() => {
       const obs = new IntersectionObserver(([entry]) => {
         if (entry.isIntersecting) { setVisible(true); obs.disconnect(); }
       }, { rootMargin: '200px' });
       if (ref.current) obs.observe(ref.current);
       return () => obs.disconnect();
     }, []);
     return <img ref={ref} src={visible ? src : PLACEHOLDER_SRC} alt={alt} />;
   };
2. Placeholder: tiny base64 grey square lub blurhash (opt).
3. Gallery.tsx — zastąp <img> przez <LazyThumbnail>.

ACCEPTANCE: vitest mock IntersectionObserver + manual network inspection.

DELIVERABLE: diff + 1 vitest.
```

## Labels

`P0, phase-2, owner-luq, frontend, performance`

## Definition of Done

- [ ] Kod scalony do `main` przez PR
- [ ] PR ma minimum 1 approval od drugiej osoby
- [ ] CI zielone (lint + pytest + npm build, jeśli istnieje)
- [ ] Acceptance criteria spełnione i sprawdzone manualnie
- [ ] Dokumentacja zaktualizowana jeśli zmiana publicznego API
- [ ] CHANGELOG.md ma wpis w sekcji `[Unreleased]`

---
*Generated from `docs/PRODUCTION_ROADMAP.md` task `2.3.2`.*
