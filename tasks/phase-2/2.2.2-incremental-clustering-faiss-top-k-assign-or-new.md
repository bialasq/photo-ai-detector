# [2.2.2] Incremental clustering: FAISS top-k → assign or new

> **Owner:** Luq | **Hours:** 8 | **Priority:** P0 | **Phase:** 2 | **Source:** PDF

## Context

Dla NOWEJ twarzy: query FAISS top-k → similarity > threshold + same cluster → assign. Inaczej: pending dla następnego DBSCAN re-cluster.

## Files affected
- `ai_core.py`
- `tests/test_incremental_clustering.py`

## Dependencies
- 2.2.1

## Acceptance Criteria

- [ ] incremental_assign(emb, threshold=0.7) → cluster_id or None
- [ ] Jeśli najbliższe k=5 mają SAME cluster_id → assign do tego cluster
- [ ] Mixed clusters w top-k → pending (None → wymaga full DBSCAN)
- [ ] Threshold konfigurowalny env PHOTO_ORGANIZER_INCREMENTAL_THRESHOLD
- [ ] 5 pytest: clear assign, mixed, below threshold, named only, no candidates

## Cursor Prompt

> Wklej 1:1 do Cursor Composer / Agent mode. Pamiętaj że `.cursorrules` w roocie repo jest aktywne.

```
ROLA: Senior ML engineer (clustering).

TASK: Incremental clustering via FAISS.

WYMAGANIA:
1. ai_core.py:
   def incremental_assign(emb: np.ndarray, threshold: float = 0.7) -> Optional[int]:
     candidates = vector_store.search(emb, k=5)
     # filter by threshold
     valid = [(fid, sim) for fid, sim in candidates if sim > threshold]
     if not valid: return None
     # check if all same cluster
     cluster_ids = [get_cluster_for_face(fid) for fid, _ in valid]
     cluster_ids = [c for c in cluster_ids if c is not None]
     if len(set(cluster_ids)) == 1: return cluster_ids[0]
     return None  # mixed → wait for full DBSCAN
2. Threshold z env PHOTO_ORGANIZER_INCREMENTAL_THRESHOLD (default 0.7).
3. Po skanie: dla face z incremental_assign=None → pending flag → next full re-cluster.

NIE WOLNO:
- Auto-assign jeśli mixed (ryzyko fałszywego pozytywu — łączenie różnych osób).
- Threshold < 0.5 (zbyt liberalne).

ACCEPTANCE: 5 pytest.

DELIVERABLE: diff + 5 testów.
```

## Labels

`P0, phase-2, owner-luq, backend, ml`

## Definition of Done

- [ ] Kod scalony do `main` przez PR
- [ ] PR ma minimum 1 approval od drugiej osoby
- [ ] CI zielone (lint + pytest + npm build, jeśli istnieje)
- [ ] Acceptance criteria spełnione i sprawdzone manualnie
- [ ] Dokumentacja zaktualizowana jeśli zmiana publicznego API
- [ ] CHANGELOG.md ma wpis w sekcji `[Unreleased]`

---
*Generated from `docs/PRODUCTION_ROADMAP.md` task `2.2.2`.*
