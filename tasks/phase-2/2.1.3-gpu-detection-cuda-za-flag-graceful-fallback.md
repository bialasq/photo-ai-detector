# [2.1.3] GPU detection (CUDA) za flagą + graceful fallback

> **Owner:** Luq | **Hours:** 6 | **Priority:** P1 | **Phase:** 2 | **Source:** PDF

## Context

Część userów ma GPU. Jeśli PHOTO_ORGANIZER_GPU=1 → spróbuj CUDA. Jeśli CUDA nieobecny / TF nie widzi → log warning + fallback CPU. v1.0.0: flag opt-in, default off (stabilność > speed).

## Files affected
- `gpu.py`
- `ai_core.py`
- `docs/GPU_SETUP.md`

## Dependencies
_None_

## Acceptance Criteria

- [ ] gpu.py: detect_gpu() → bool; configure_tensorflow_gpu()
- [ ] PHOTO_ORGANIZER_GPU=1 → detect_gpu() called; jeśli False → fallback CPU + log warning
- [ ] Default (no flag) → CPU forever
- [ ] docs/GPU_SETUP.md: jak zainstalować CUDA, jakie wersje TF
- [ ] Benchmark GPU vs CPU (jeśli dostępne hardware u Luqa) — co najmniej dokumentacja

## Cursor Prompt

> Wklej 1:1 do Cursor Composer / Agent mode. Pamiętaj że `.cursorrules` w roocie repo jest aktywne.

```
ROLA: Senior ML + DevOps engineer.

TASK: Opt-in GPU support.

WYMAGANIA:
1. gpu.py:
   def detect_gpu() -> bool:
     import tensorflow as tf
     gpus = tf.config.list_physical_devices('GPU')
     return len(gpus) > 0
   
   def configure_tensorflow_gpu():
     # memory_growth=True, limit memory
2. ai_core.py startup:
   gpu_enabled = os.getenv('PHOTO_ORGANIZER_GPU', '0') == '1'
   if gpu_enabled and detect_gpu():
     configure_tensorflow_gpu(); logger.info('GPU enabled')
   elif gpu_enabled:
     logger.warning('PHOTO_ORGANIZER_GPU=1 but no GPU detected, falling back to CPU')
3. docs/GPU_SETUP.md:
   - Wymagane: NVIDIA driver, CUDA Toolkit (sprawdź tensorflow.org/install/source#tested_build_configurations)
   - tensorflow + tensorflow-gpu (jeśli wersja TF tego wymaga)
   - Test: python -c 'import tensorflow as tf; print(tf.config.list_physical_devices("GPU"))'

NIE WOLNO:
- Hard-require CUDA — większość userów ma tylko CPU.
- Crashować przy braku GPU mimo flagi (graceful fallback).

ACCEPTANCE: manual test z GPU (Luq local) + bez GPU (CI ubuntu).

DELIVERABLE: gpu.py + diff + docs.
```

## Labels

`P1, phase-2, owner-luq, backend, ml, performance`

## Definition of Done

- [ ] Kod scalony do `main` przez PR
- [ ] PR ma minimum 1 approval od drugiej osoby
- [ ] CI zielone (lint + pytest + npm build, jeśli istnieje)
- [ ] Acceptance criteria spełnione i sprawdzone manualnie
- [ ] Dokumentacja zaktualizowana jeśli zmiana publicznego API
- [ ] CHANGELOG.md ma wpis w sekcji `[Unreleased]`

---
*Generated from `docs/PRODUCTION_ROADMAP.md` task `2.1.3`.*
