# [2.1.1] Batch detection 32 obrazów w ai_core

> **Owner:** Grzesiek | **Hours:** 10 | **Priority:** P0 | **Phase:** 2 | **Source:** PDF

## Context

Per-image DeepFace = wąskie gardło. Batch 32 → 2-5× speedup. UWAGA: DeepFace może nie wspierać native batch — sprawdź dokumentację PRZED kodowaniem. Fallback: ThreadPoolExecutor (NIE process pool — TF + PyInstaller).

## Files affected
- `ai_core.py`
- `tests/test_batch_detection.py`
- `docs/BENCHMARKS.md`

## Dependencies
- 2.0.1

## Acceptance Criteria

- [ ] detect_faces_batch(image_paths: list[Path]) → list[FaceResult]
- [ ] Sprawdzone czy DeepFace native batch działa w wersji z requirements
- [ ] Jeśli NIE → ThreadPoolExecutor(max_workers=4) z documented uzasadnieniem
- [ ] Memory: 32 obrazy × ~1080p × 3ch = ~200 MB peak; OOM retry z batch_size //= 2
- [ ] Benchmark: 1k zdjęć ≥ 2× speedup vs baseline (z 2.0.1)
- [ ] Manual: RAM peak < 1.5 GB podczas skanu 1k

## Cursor Prompt

> Wklej 1:1 do Cursor Composer / Agent mode. Pamiętaj że `.cursorrules` w roocie repo jest aktywne.

```
ROLA: Senior ML engineer (DeepFace, TensorFlow).

TASK: Batch detection 32 obrazów.

WYMAGANIA:
1. Otwórz ai_core.py — obecna pętla per-image.
2. ZWERYFIKUJ czy DeepFace.represent/analyze wspiera batch:
   - Sprawdź github.com/serengil/deepface README + DeepFace.py source.
   - Jeśli NIE — udokumentuj w komentarzu i zrób ThreadPoolExecutor(max_workers=4).
   - NIE PROCESS POOL — TensorFlow + PyInstaller = problems.
3. Nowa funkcja:
   def detect_faces_batch(image_paths: list[Path]) -> list[FaceResult]:
     with ThreadPoolExecutor(max_workers=4) as ex:
       results = list(ex.map(detect_one, image_paths))
     return results
4. OOM handling:
   try:
     return detect_faces_batch(paths, batch_size=32)
   except MemoryError:
     logger.warning('OOM at batch=32, retry batch=16')
     return detect_faces_batch(paths, batch_size=16)
5. Benchmark before/after — porównaj z baseline z 2.0.1.

NIE WOLNO:
- Wymyślać API DeepFace, którego nie ma. Jeśli nie ma batch_size param — fallback thread pool.
- ProcessPoolExecutor (TF + PyInstaller crash).

ACCEPTANCE: pytest test_batch_detection_returns_correct_count + benchmark ≥ 2× + decyzja batch vs threads w PR.

DELIVERABLE: diff + benchmark + decision.
```

## Labels

`P0, phase-2, owner-grzesiek, backend, ml, performance`

## Definition of Done

- [ ] Kod scalony do `main` przez PR
- [ ] PR ma minimum 1 approval od drugiej osoby
- [ ] CI zielone (lint + pytest + npm build, jeśli istnieje)
- [ ] Acceptance criteria spełnione i sprawdzone manualnie
- [ ] Dokumentacja zaktualizowana jeśli zmiana publicznego API
- [ ] CHANGELOG.md ma wpis w sekcji `[Unreleased]`

---
*Generated from `docs/PRODUCTION_ROADMAP.md` task `2.1.1`.*
