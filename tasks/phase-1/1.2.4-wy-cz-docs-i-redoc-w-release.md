# [1.2.4] Wyłącz /docs i /redoc w release

> **Owner:** Grzesiek | **Hours:** 1 | **Priority:** P0 | **Phase:** 1 | **Source:** Cursor

## Context

FastAPI auto /docs (Swagger) i /redoc ujawniają powierzchnię API. W dev OK, w release wyłączyć. OpenAPI JSON do pliku w support bundle.

## Files affected
- `main.py`

## Dependencies
- 1.2.1

## Acceptance Criteria

- [ ] FastAPI(docs_url=None, redoc_url=None, openapi_url=None) w release
- [ ] DEV_MODE → docs_url='/docs' (jak dziś)
- [ ] Manual: release build → curl /docs → 404
- [ ] OpenAPI export do pliku przez script scripts/export_openapi.py (dla support)

## Cursor Prompt

> Wklej 1:1 do Cursor Composer / Agent mode. Pamiętaj że `.cursorrules` w roocie repo jest aktywne.

```
ROLA: Senior FastAPI engineer.

TASK: Wyłącz /docs, /redoc, /openapi.json w release.

WYMAGANIA:
1. main.py — FastAPI() init:
   ```python
   DEV_MODE = os.getenv('PHOTO_ORGANIZER_DEV', '0') == '1'
   app = FastAPI(
       docs_url='/docs' if DEV_MODE else None,
       redoc_url='/redoc' if DEV_MODE else None,
       openapi_url='/openapi.json' if DEV_MODE else None,
   )
   ```
2. scripts/export_openapi.py — uruchamiany w CI, wypluwa openapi.json do support bundle (docs/api/openapi.json). Sprawdź w buildzie.

ACCEPTANCE: manual curl /docs w release → 404.

DELIVERABLE: diff + script.
```

## Labels

`P0, phase-1, owner-grzesiek, backend, security`

## Definition of Done

- [ ] Kod scalony do `main` przez PR
- [ ] PR ma minimum 1 approval od drugiej osoby
- [ ] CI zielone (lint + pytest + npm build, jeśli istnieje)
- [ ] Acceptance criteria spełnione i sprawdzone manualnie
- [ ] Dokumentacja zaktualizowana jeśli zmiana publicznego API
- [ ] CHANGELOG.md ma wpis w sekcji `[Unreleased]`

---
*Generated from `docs/PRODUCTION_ROADMAP.md` task `1.2.4`.*
