# [1.2.2] Przenieś DB do %AppData%\com.photo.organizer\organizer.db [KRYTYCZNE]

> **Owner:** Grzesiek | **Hours:** 4 | **Priority:** P0 | **Phase:** 1 | **Source:** Cursor

## Context

organizer.db w produkcji MUSI być w %APPDATA%\com.photo.organizer\. Dziś jest w CWD sidecara lub katalogu repo. To powoduje że uninstall potencjalnie zostawia plik, a różne instancje mogą wchodzić sobie w drogę.

## Files affected
- `database.py`
- `src-tauri/src/lib.rs`
- `tests/test_db_path.py`

## Dependencies
_None_

## Acceptance Criteria

- [ ] get_db_path() respektuje PHOTO_ORGANIZER_DB_PATH env override
- [ ] Windows: %APPDATA%\com.photo.organizer\organizer.db
- [ ] macOS: ~/Library/Application Support/com.photo.organizer/
- [ ] Linux: ~/.local/share/com.photo.organizer/
- [ ] Tauri lib.rs ustawia PHOTO_ORGANIZER_DB_PATH przy spawn sidecara z tauri::api::path::app_data_dir
- [ ] Decyzja migracji starych userów (Luq): kopia z notyfikacją vs fresh start (w PR comment)
- [ ] .gitignore: organizer.db (dla pewności)
- [ ] pytest: test_db_path_windows_uses_appdata (mock platform + env)

## Cursor Prompt

> Wklej 1:1 do Cursor Composer / Agent mode. Pamiętaj że `.cursorrules` w roocie repo jest aktywne.

```
ROLA: Senior Python + Tauri engineer.

TASK: organizer.db w produkcji w %APPDATA%\com.photo.organizer\organizer.db.

WYMAGANIA:
1. database.py:
   ```python
   def get_db_path() -> Path:
       override = os.getenv('PHOTO_ORGANIZER_DB_PATH')
       if override:
           return Path(override)
       if sys.platform == 'win32':
           base = Path(os.environ['APPDATA']) / 'com.photo.organizer'
       elif sys.platform == 'darwin':
           base = Path.home() / 'Library/Application Support/com.photo.organizer'
       else:
           base = Path.home() / '.local/share/com.photo.organizer'
       base.mkdir(parents=True, exist_ok=True)
       return base / 'organizer.db'
   ```
2. src-tauri/src/lib.rs: przy spawn sidecara w release ustaw env PHOTO_ORGANIZER_DB_PATH = tauri::api::path::app_data_dir(...) / 'organizer.db'.
3. Migracja userów: jeśli plik organizer.db jest w CWD i w AppData pusto → ZAPYTAJ LUQA w PR czy v1.0.0 ma kopię z notyfikacją czy fresh start.
4. .gitignore: organizer.db.

NIE WOLNO: hardcodować ścieżki bez get_db_path().

ACCEPTANCE: pytest + manual Tauri release run + verify %AppData%.

DELIVERABLE: diff + decyzja migracji w PR description.
```

## Labels

`P0, phase-1, owner-grzesiek, backend, tauri, data, blocker`

## Definition of Done

- [ ] Kod scalony do `main` przez PR
- [ ] PR ma minimum 1 approval od drugiej osoby
- [ ] CI zielone (lint + pytest + npm build, jeśli istnieje)
- [ ] Acceptance criteria spełnione i sprawdzone manualnie
- [ ] Dokumentacja zaktualizowana jeśli zmiana publicznego API
- [ ] CHANGELOG.md ma wpis w sekcji `[Unreleased]`

---
*Generated from `docs/PRODUCTION_ROADMAP.md` task `1.2.2`.*
