# [1.1.4] React Error Boundaries (App, Gallery, PeopleGrid)

> **Owner:** Luq | **Hours:** 5 | **Priority:** P0 | **Phase:** 1 | **Source:** PDF

## Context

Aktualnie nieobsłużony exception w komponencie wywala cały tree do białego ekranu. Fallback UI per scope + log do backendu POST /api/v1/log-error.

## Files affected
- `src/components/ErrorBoundary.tsx`
- `src/App.tsx`
- `src/test/ErrorBoundary.test.tsx`

## Dependencies
- 1.1.3

## Acceptance Criteria

- [ ] Class component ErrorBoundary z props {fallback?, scope}
- [ ] componentDidCatch → POST /api/v1/log-error
- [ ] Wrappery: <ErrorBoundary scope='app'>, scope='gallery', scope='people'
- [ ] Fallback domyślny: 'Something broke in {scope}. Try again or restart the app.'
- [ ] Vitest test: throw w child → fallback rendered, NOT white screen
- [ ] Manual: throw Error('boom') w useEffect Gallery → fallback + log w backend.log

## Cursor Prompt

> Wklej 1:1 do Cursor Composer / Agent mode. Pamiętaj że `.cursorrules` w roocie repo jest aktywne.

```
ROLA: Senior React engineer.

TASK: Error Boundaries dla `src/` (React UI).

WYMAGANIA:
1. src/components/ErrorBoundary.tsx — Class component (jedyny wyjątek od reguły 'tylko function components' bo Error Boundaries WYMAGAJĄ class).
   - Props: { fallback?: ReactNode, scope: string, children: ReactNode }
   - State: { error: Error | null, errorInfo: ErrorInfo | null }
   - static getDerivedStateFromError + componentDidCatch.
   - componentDidCatch → fetch('/api/v1/log-error', { method: 'POST', body: JSON.stringify({scope, message: error.message, stack: errorInfo.componentStack}) }).
2. App.tsx: 3 wrappery scope='app' (top), scope='gallery', scope='people'.
3. Vitest: render <ErrorBoundary scope='test'><Thrower /></ErrorBoundary> → expect fallback text.
4. Backend endpoint /api/v1/log-error: stub akceptujący {scope, message, stack} i logujący jako ERROR z prefix 'frontend:' (Grzesiek skończy w 1.1.2; tu interface).

NIE WOLNO:
- Console.log w fallback (cichy).
- Catch w boundary i 'połknąć' błąd bez log.

ACCEPTANCE: vitest passing + manual screenshot fallback UI.

DELIVERABLE: diff + screenshot + 1 vitest.
```

## Labels

`P0, phase-1, owner-luq, frontend, stability`

## Definition of Done

- [ ] Kod scalony do `main` przez PR
- [ ] PR ma minimum 1 approval od drugiej osoby
- [ ] CI zielone (lint + pytest + npm build, jeśli istnieje)
- [ ] Acceptance criteria spełnione i sprawdzone manualnie
- [ ] Dokumentacja zaktualizowana jeśli zmiana publicznego API
- [ ] CHANGELOG.md ma wpis w sekcji `[Unreleased]`

---
*Generated from `docs/PRODUCTION_ROADMAP.md` task `1.1.4`.*
