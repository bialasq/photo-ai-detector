# [1.2.1] Gate /api/dev/* za PHOTO_ORGANIZER_DEV=1 [KRYTYCZNE]

> **Owner:** Grzesiek | **Hours:** 3 | **Priority:** P0 | **Phase:** 1 | **Source:** Cursor

## Context

BEZPIECZEŃSTWO KRYTYCZNE. Endpointy /api/dev/* (m.in. /api/dev/reset-library) MUSZĄ być NIEDOSTĘPNE w buildzie release. Dziś są zawsze włączone. Bez tego nie ma release.

## Files affected
- `main.py`
- `src-tauri/src/lib.rs`
- `tests/integration/test_dev_endpoints.py`

## Dependencies
_None_

## Acceptance Criteria

- [ ] Release build: curl /api/dev/reset-library → 404
- [ ] Dev build (PHOTO_ORGANIZER_DEV=1): curl /api/dev/reset-library → 200
- [ ] Default DEV_MODE=False (NIE True)
- [ ] Log warning przy starcie z DEV=1: 'DEV endpoints ENABLED — do not use in production'
- [ ] Tauri lib.rs: PHOTO_ORGANIZER_DEV przekazywane do sidecara TYLKO w cfg!(debug_assertions)
- [ ] 2 pytest: test_dev_endpoints_disabled_by_default + test_dev_endpoints_enabled_with_env
- [ ] Manual: zbuduj release sidecar, sprawdź 404

## Cursor Prompt

> Wklej 1:1 do Cursor Composer / Agent mode. Pamiętaj że `.cursorrules` w roocie repo jest aktywne.

```
ROLA: Senior Python/FastAPI engineer (security-conscious).

TASK: Endpointy /api/dev/* MUSZĄ być NIEDOSTĘPNE w release.

WYMAGANIA:
1. Otwórz main.py — znajdź rejestrację routów /api/dev/*.
2. Przed rejestracją routera dev:
   ```python
   import os
   DEV_MODE = os.getenv('PHOTO_ORGANIZER_DEV', '0') == '1'
   if DEV_MODE:
       app.include_router(dev_router, prefix='/api/dev')
       logger.warning('DEV endpoints ENABLED — do not use in production')
   ```
3. W src-tauri/src/lib.rs (sidecar spawn): NIE przekazuj PHOTO_ORGANIZER_DEV=1 w release build.
   Użyj `cfg!(debug_assertions)` lub feature flag.
4. Test E2E manual: build release sidecar (PyInstaller), uruchom, curl http://127.0.0.1:8000/api/dev/reset-library → 404.

NIE WOLNO:
- Default DEV_MODE=True.
- Trustować X-Dev-Mode headerowi. ENV ONLY.

ACCEPTANCE:
- 2 pytest: disabled by default + enabled with env
- Manual release build → curl /api/dev/reset-library → 404

DELIVERABLE: diff main.py + lib.rs + 2 testy + output curl.
```

## Labels

`P0, phase-1, owner-grzesiek, backend, security, blocker`

## Definition of Done

- [ ] Kod scalony do `main` przez PR
- [ ] PR ma minimum 1 approval od drugiej osoby
- [ ] CI zielone (lint + pytest + npm build, jeśli istnieje)
- [ ] Acceptance criteria spełnione i sprawdzone manualnie
- [ ] Dokumentacja zaktualizowana jeśli zmiana publicznego API
- [ ] CHANGELOG.md ma wpis w sekcji `[Unreleased]`

---
*Generated from `docs/PRODUCTION_ROADMAP.md` task `1.2.1`.*
