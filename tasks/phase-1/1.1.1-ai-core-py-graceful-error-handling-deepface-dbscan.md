# [1.1.1] ai_core.py graceful error handling (DeepFace + DBSCAN)

> **Owner:** Grzesiek | **Hours:** 8 | **Priority:** P0 | **Phase:** 1 | **Source:** PDF
> **Status:** DONE — zmergowane do `main` (2026-06). Sekwencja sesji: `1.1.1-EXECUTION-PROMPTS.md`.

## Context

Każda interakcja z DeepFace (analyze, represent) i sklearn DBSCAN może rzucić ValueError, MemoryError, cv2.error, np.linalg.LinAlgError, FileNotFoundError. Dziś nieobsłużone — wywala cały skan. Wprowadzić hierarchię wyjątków + per-image isolation.

## Files affected
- `ai_core.py`
- `main.py`
- `tests/test_ai_core_errors.py`

## Dependencies
_None_

## Acceptance Criteria

- [ ] Hierarchia: AICoreError → FaceDetectionError, EmbeddingError, ClusteringError
- [ ] Wszystkie zewnętrzne wywołania ML owinięte w try/except SPECYFICZNYM wyjątkiem
- [ ] Pojedyncza twarz padająca NIE wywala batcha — log warning, processed=1, has_faces=0
- [ ] DBSCAN exception → state.last_error, NIE crash
- [ ] 4 pytest dla każdego typu wyjątku → odpowiedni AICoreError z context
- [ ] Manual: skan z 0-byte JPG → batch leci dalej

## Cursor Prompt

> Wklej 1:1 do Cursor Composer / Agent mode. Pamiętaj że `.cursorrules` w roocie repo jest aktywne.

```
ROLA: Senior Python engineer specjalizujący się w pipeline'ach ML.

TASK: Dodaj graceful error handling w ai_core.py.
Każda interakcja z DeepFace (analyze, represent) i sklearn.cluster.DBSCAN może rzucić:
ValueError (model not loaded), MemoryError, cv2.error, np.linalg.LinAlgError, FileNotFoundError.

WYMAGANIA:
1. Otwórz ai_core.py i przeczytaj GO CAŁY.
2. Otwórz main.py — znajdź wywołania z ai_core, sprawdź jak propagujemy błędy.
3. Wprowadź klasy wyjątków w ai_core.py:
   - class AICoreError(Exception)
   - class FaceDetectionError(AICoreError)
   - class EmbeddingError(AICoreError)
   - class ClusteringError(AICoreError)
4. Każde zewnętrzne wywołanie ML owiń try/except SPECYFICZNYM wyjątkiem (NIE goły except) → log z context (path, batch_idx) → re-raise jako AICoreError.
5. W main.py: catch AICoreError → 500 ze standardowym payloadem {error, code: 'ai_core_failure', hint}.
6. Pojedyncza twarz padająca NIE wywala batcha — log warning, processed=1, has_faces=0, lećmy dalej.
7. DBSCAN exception → catch w main, flaguj ScanProgressState.last_error.

NIE WOLNO:
- Dodawać nowych zależności.
- Refaktorować 'obok' (struktura modułów zostaje).
- Tłumić błędów cicho — zawsze log z level ERROR + traceback.

TESTUJ:
- pytest tests/test_ai_core_errors.py (4 testy)
- Manual: skan z 1 zepsutym JPG (0 bajtów) — czy batch leci dalej?

DELIVERABLE: diff 3 plików + output pytest + pre-PR checklist z .cursorrules.
```

## Labels

`P0, phase-1, owner-grzesiek, backend, stability`

## Definition of Done

- [ ] Kod scalony do `main` przez PR
- [ ] PR ma minimum 1 approval od drugiej osoby
- [ ] CI zielone (lint + pytest + npm build, jeśli istnieje)
- [ ] Acceptance criteria spełnione i sprawdzone manualnie
- [ ] Dokumentacja zaktualizowana jeśli zmiana publicznego API
- [ ] CHANGELOG.md ma wpis w sekcji `[Unreleased]`

---
*Generated from `docs/PRODUCTION_ROADMAP.md` task `1.1.1`.*
