# [1.2.3] Path traversal validation w endpointach skanu

> **Owner:** Luq | **Hours:** 4 | **Priority:** P0 | **Phase:** 1 | **Source:** Cursor

## Context

Endpointy skanu przyjmują path od użytkownika. Walidacja minimalna. Reject: ..\, symlinki out-of-tree, nieistniejące, pliki (musi być katalog). Warn: root dysku (wymagaj X-Confirm-Whole-Disk: 1 header).

## Files affected
- `path_validation.py`
- `main.py`
- `src/api/scan.ts`
- `tests/test_path_validation.py`

## Dependencies
_None_

## Acceptance Criteria

- [ ] validate_scan_path(raw: str) → Path z 5 rejekcjami
- [ ] Frontend używa tauri-plugin-dialog (NIE arbitrary string)
- [ ] 5 pytest: '..', symlink, nieistnieje, plik nie katalog, root + missing header
- [ ] Manual: POST /api/scan-start {path: '..\\Windows'} → 400
- [ ] Logowanie ścieżki tylko jako hash w response error

## Cursor Prompt

> Wklej 1:1 do Cursor Composer / Agent mode. Pamiętaj że `.cursorrules` w roocie repo jest aktywne.

```
ROLA: Senior security engineer.

TASK: Path traversal hardening.

WYMAGANIA:
1. path_validation.py:
   - class PathValidationError(Exception)
   - def validate_scan_path(raw: str, allow_whole_disk: bool = False) -> Path:
     - Reject: '..' w segmentach
     - Resolve symlinks; reject jeśli target out-of-original-tree
     - Reject: nieistniejące
     - Reject: nie-katalogi
     - Reject: root dysku (C:\, D:\, /) jeśli not allow_whole_disk
2. main.py POST /api/scan-start:
   - parse header X-Confirm-Whole-Disk
   - validate_scan_path(body.path, allow_whole_disk=header == '1')
   - except PathValidationError → 400 PATH_INVALID
3. Frontend: użyj tauri-plugin-dialog 'open' z directory: true. NIE wysyłaj arbitrary stringów.
4. 5 pytest dla każdego case + 1 positive.

NIE WOLNO:
- Logować surowej path w response (privacy + path leak).
- 'Naprawiać' path (e.g. removing ..) — REJECTOWAĆ. Naprawianie = bypass risk.

ACCEPTANCE: 6 testów + tabela case'ów w PR.

DELIVERABLE: path_validation.py + diff main.py + 6 testów.
```

## Labels

`P0, phase-1, owner-luq, backend, security`

## Definition of Done

- [ ] Kod scalony do `main` przez PR
- [ ] PR ma minimum 1 approval od drugiej osoby
- [ ] CI zielone (lint + pytest + npm build, jeśli istnieje)
- [ ] Acceptance criteria spełnione i sprawdzone manualnie
- [ ] Dokumentacja zaktualizowana jeśli zmiana publicznego API
- [ ] CHANGELOG.md ma wpis w sekcji `[Unreleased]`

---
*Generated from `docs/PRODUCTION_ROADMAP.md` task `1.2.3`.*
