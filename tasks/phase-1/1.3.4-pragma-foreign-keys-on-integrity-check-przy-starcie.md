# [1.3.4] PRAGMA foreign_keys=ON + integrity_check przy starcie

> **Owner:** Luq | **Hours:** 2 | **Priority:** P0 | **Phase:** 1 | **Source:** Cursor

## Context

SQLite default ma foreign_keys=OFF. Trzeba ON per-connection. Przy starcie sidecara: PRAGMA integrity_check; jeśli != 'ok' → log ERROR + odmowa startu.

## Files affected
- `database.py`
- `main.py`
- `tests/integration/test_db_integrity.py`

## Dependencies
- 1.3.2

## Acceptance Criteria

- [ ] Każde get_connection() ustawia PRAGMA foreign_keys=ON
- [ ] Startup: PRAGMA integrity_check; result != 'ok' → log ERROR + raise SystemExit
- [ ] 1 pytest: corrupt DB → startup fails gracefully
- [ ] Manual: orphan face (photo deleted, face ref) → foreign key violation

## Cursor Prompt

> Wklej 1:1 do Cursor Composer / Agent mode. Pamiętaj że `.cursorrules` w roocie repo jest aktywne.

```
ROLA: Senior SQLite engineer.

TASK: PRAGMA foreign_keys=ON wszędzie + integrity_check przy starcie.

WYMAGANIA:
1. database.py get_connection() / connect():
   conn = sqlite3.connect(db_path)
   conn.execute('PRAGMA foreign_keys=ON')
   conn.execute('PRAGMA journal_mode=WAL')  # bonus dla concurrency
2. main.py startup:
   result = conn.execute('PRAGMA integrity_check').fetchone()[0]
   if result != 'ok': logger.error(f'DB corruption: {result}'); raise SystemExit(1)
3. pytest: stwórz DB z orphan face row → assert IntegrityError przy próbie wstawienia.

NIE WOLNO: ignorować integrity_check failure (data corruption silently).

ACCEPTANCE: 1 test + manual smoke.

DELIVERABLE: diff + test.
```

## Labels

`P0, phase-1, owner-luq, backend, database`

## Definition of Done

- [ ] Kod scalony do `main` przez PR
- [ ] PR ma minimum 1 approval od drugiej osoby
- [ ] CI zielone (lint + pytest + npm build, jeśli istnieje)
- [ ] Acceptance criteria spełnione i sprawdzone manualnie
- [ ] Dokumentacja zaktualizowana jeśli zmiana publicznego API
- [ ] CHANGELOG.md ma wpis w sekcji `[Unreleased]`

---
*Generated from `docs/PRODUCTION_ROADMAP.md` task `1.3.4`.*
