# [1.3.1] DB indeksy: faces(person_id), photos(processed, has_faces)

> **Owner:** Luq | **Hours:** 4 | **Priority:** P0 | **Phase:** 1 | **Source:** PDF+Cursor

## Context

Galeria i identyfikacja klastra robią SCAN TABLE. Cel: <100ms p95 przy 100k twarzy. EXPLAIN QUERY PLAN PRZED indeksami → uzasadnij każdy indeks.

## Files affected
- `migrations/003_indexes.sql`
- `tests/perf/test_query_plans.py`
- `docs/DB_INDEXES.md`

## Dependencies
- 1.3.2

## Acceptance Criteria

- [ ] Każdy SELECT galerii używa indeksu (no SCAN TABLE)
- [ ] Tabela w PR description: query → plan before → plan after
- [ ] pytest perf: 5 query → assert 'SCAN' not in plan
- [ ] Benchmark: get_all_photos(faceless_only=True) na 50k zdjęć < 100ms
- [ ] docs/DB_INDEXES.md — lista indeksów + uzasadnienie

## Cursor Prompt

> Wklej 1:1 do Cursor Composer / Agent mode. Pamiętaj że `.cursorrules` w roocie repo jest aktywne.

```
ROLA: Senior DBA / SQLite engineer.

TASK: Indeksy DB pod query galerii i identyfikacji klastra.

WYMAGANIA:
1. Otwórz database.py — wszystkie SELECT.
2. Uruchom (test fixture 10k twarzy): EXPLAIN QUERY PLAN dla każdego.
3. Zidentyfikuj SCAN TABLE → uzasadnij indeks.
4. migrations/003_indexes.sql:
   ```sql
   CREATE INDEX IF NOT EXISTS idx_faces_person_id ON faces(person_id);
   CREATE INDEX IF NOT EXISTS idx_faces_photo_id ON faces(photo_id);
   CREATE INDEX IF NOT EXISTS idx_photos_processed ON photos(processed, has_faces);
   CREATE INDEX IF NOT EXISTS idx_photos_path ON photos(path);
   ```
   (dokładny zestaw zależy od schema — zweryfikuj PRZED pisaniem)
5. Re-run EXPLAIN — wszystkie SELECTy galerii MUSZĄ używać USE INDEX.

NIE WOLNO:
- Indeksować na zapas — każdy indeks = wolniejsze INSERT.
- Tylko indeksy uzasadnione EXPLAIN.

ACCEPTANCE: pytest tests/perf/test_query_plans.py + tabela query.

DELIVERABLE: migracja + tabela plan w PR + docs/DB_INDEXES.md.
```

## Labels

`P0, phase-1, owner-luq, backend, database, performance`

## Definition of Done

- [ ] Kod scalony do `main` przez PR
- [ ] PR ma minimum 1 approval od drugiej osoby
- [ ] CI zielone (lint + pytest + npm build, jeśli istnieje)
- [ ] Acceptance criteria spełnione i sprawdzone manualnie
- [ ] Dokumentacja zaktualizowana jeśli zmiana publicznego API
- [ ] CHANGELOG.md ma wpis w sekcji `[Unreleased]`

---
*Generated from `docs/PRODUCTION_ROADMAP.md` task `1.3.1`.*
