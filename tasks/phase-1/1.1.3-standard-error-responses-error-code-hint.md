# [1.1.3] Standard error responses {error, code, hint}

> **Owner:** Grzesiek | **Hours:** 4 | **Priority:** P0 | **Phase:** 1 | **Source:** PDF

## Context

Wszystkie endpointy zwracają błędy w jednolitym formacie. Frontend ma jeden helper który ten format rozumie. Pydantic ErrorResponse model.

## Files affected
- `errors.py`
- `main.py`
- `src/api/errors.ts`

## Dependencies
- 1.1.1

## Acceptance Criteria

- [ ] Pydantic model ErrorResponse(error: str, code: str, hint: Optional[str], details: Optional[dict])
- [ ] FastAPI exception handler globalny mapujący wyjątki → ErrorResponse
- [ ] Tabela kodów w docs/API.md (np. ai_core_failure, db_locked, path_invalid, validation_error)
- [ ] Frontend api/errors.ts ma typ ApiError zgodny z backend
- [ ] pytest: 5 endpointów × 1 błąd każdy → kontrakt sprawdzony

## Cursor Prompt

> Wklej 1:1 do Cursor Composer / Agent mode. Pamiętaj że `.cursorrules` w roocie repo jest aktywne.

```
ROLA: Senior FastAPI engineer.

TASK: Zunifikuj format błędów API.

WYMAGANIA:
1. errors.py:
   - ErrorCode enum: AI_CORE_FAILURE, DB_LOCKED, PATH_INVALID, VALIDATION_ERROR, NOT_FOUND, INTERNAL_ERROR, SCAN_IN_PROGRESS, SCAN_CANCELLED.
   - Pydantic v2 ErrorResponse(error: str, code: ErrorCode, hint: Optional[str], details: Optional[dict]).
2. main.py: register exception handlers:
   - AICoreError → 500 ErrorResponse(code=AI_CORE_FAILURE)
   - sqlite3.IntegrityError → 409 DB_LOCKED
   - PathValidationError (z 1.2.3) → 400 PATH_INVALID
   - RequestValidationError (Pydantic) → 422 VALIDATION_ERROR
   - Exception generic → 500 INTERNAL_ERROR (z tracebackiem w log, NIE w response)
3. src/api/errors.ts: TypeScript types + helper isApiError().
4. docs/API_ERROR_CODES.md: tabela code → http_status → meaning → user-facing hint.

NIE WOLNO:
- Zwracać traceback w response (security).
- Używać innego shape w starych endpointach 'na razie' — refaktor wszystkich.

ACCEPTANCE: 5 testów endpoint × error path + tabela kodów + frontend types.

DELIVERABLE: diff + docs/API_ERROR_CODES.md.
```

## Labels

`P0, phase-1, owner-grzesiek, backend, api`

## Definition of Done

- [ ] Kod scalony do `main` przez PR
- [ ] PR ma minimum 1 approval od drugiej osoby
- [ ] CI zielone (lint + pytest + npm build, jeśli istnieje)
- [ ] Acceptance criteria spełnione i sprawdzone manualnie
- [ ] Dokumentacja zaktualizowana jeśli zmiana publicznego API
- [ ] CHANGELOG.md ma wpis w sekcji `[Unreleased]`

---
*Generated from `docs/PRODUCTION_ROADMAP.md` task `1.1.3`.*
