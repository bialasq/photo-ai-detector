# [1.3.3] Batch inserts (50-100) w transakcji

> **Owner:** Grzesiek | **Hours:** 5 | **Priority:** P0 | **Phase:** 1 | **Source:** PDF

## Context

Przy skanie 10k zdjęć ~50k face inserts. Każdy INSERT osobno = wolno. Batch 100 w transakcji + executemany.

## Files affected
- `database.py`
- `ai_core.py`
- `tests/perf/test_batch_insert.py`

## Dependencies
- 1.3.2

## Acceptance Criteria

- [ ] insert_faces_batch(conn, faces: list[FaceRow], batch_size=100) używa executemany w transakcji
- [ ] ai_core.scan_directory akumuluje wyniki batch_size i flushuje co 100
- [ ] Benchmark: insert 5000 faces stary vs nowy → speedup ≥ 5×
- [ ] Failure mid-batch → rollback całego batcha (atomicity)

## Cursor Prompt

> Wklej 1:1 do Cursor Composer / Agent mode. Pamiętaj że `.cursorrules` w roocie repo jest aktywne.

```
ROLA: Senior SQLite engineer.

TASK: Batch insert faces (50-100 per transaction).

WYMAGANIA:
1. database.py:
   def insert_faces_batch(conn, faces: list[FaceRow], batch_size: int = 100):
     for i in range(0, len(faces), batch_size):
       chunk = faces[i:i+batch_size]
       conn.execute('BEGIN')
       try:
         conn.executemany('INSERT INTO faces (...) VALUES (?,...)', chunk_tuples)
         conn.execute('COMMIT')
       except Exception:
         conn.execute('ROLLBACK')
         raise
2. ai_core.scan_directory: akumuluj faces w buforze; flush co 100 lub na końcu batcha obrazów.
3. Benchmark: stary vs nowy na 5000 face inserts.

NIE WOLNO:
- COMMIT per row.
- Batch > 500 (memory + lock duration).

ACCEPTANCE: benchmark ≥ 5× + 1 pytest (atomicity).

DELIVERABLE: diff + benchmark before/after.
```

## Labels

`P0, phase-1, owner-grzesiek, backend, database, performance`

## Definition of Done

- [ ] Kod scalony do `main` przez PR
- [ ] PR ma minimum 1 approval od drugiej osoby
- [ ] CI zielone (lint + pytest + npm build, jeśli istnieje)
- [ ] Acceptance criteria spełnione i sprawdzone manualnie
- [ ] Dokumentacja zaktualizowana jeśli zmiana publicznego API
- [ ] CHANGELOG.md ma wpis w sekcji `[Unreleased]`

---
*Generated from `docs/PRODUCTION_ROADMAP.md` task `1.3.3`.*
