# [1.1.2] Structured logging w %AppData% z rotacją

> **Owner:** Grzesiek | **Hours:** 6 | **Priority:** P0 | **Phase:** 1 | **Source:** PDF+Cursor
> **Status:** DONE — merged to `main` (2026-06).

## Context

Zastąp wszystkie print() w backend/ strukturalnym logging z RotatingFileHandler w katalogu danych aplikacji (Windows: %AppData%\com.photo.organizer\logs\).

## Files affected
- `logging_config.py`
- `main.py`
- `ai_core.py`
- `database.py`

## Dependencies
- 1.2.2

## Acceptance Criteria

- [ ] logs/ powstaje w AppData (lub fallback ~/.photo-organizer/logs w dev)
- [ ] RotatingFileHandler: 5 plików × 10 MB
- [ ] Format JSON: {ts, level, logger, msg, ctx}
- [ ] `grep 'print(' main.py ai_core.py database.py` = 0 wyników
- [ ] Manual: po skanie 100 zdjęć — backend.log ma wpisy {scan.start, batch.done × N, scan.complete}
- [ ] NIE logujemy ścieżek użytkownika na INFO (tylko hash) — privacy

## Cursor Prompt

> Wklej 1:1 do Cursor Composer / Agent mode. Pamiętaj że `.cursorrules` w roocie repo jest aktywne.

```
ROLA: Senior Python engineer (logging, observability).

TASK: Zastąp print() strukturalnym logging z rotacją plików w %AppData%\com.photo.organizer\logs\.

WYMAGANIA:
1. Stwórz logging_config.py:
   - get_log_dir() → pathlib.Path; na Windows %APPDATA%\com.photo.organizer\logs,
     fallback ~/.photo-organizer/logs (Linux dev).
   - setup_logging(level: str) → konfiguruje root logger z:
     * RotatingFileHandler: 5 plików × 10 MB, backend.log.
     * StreamHandler na stderr w dev (env PHOTO_ORGANIZER_DEV=1).
     * Format JSON: {ts: ISO8601, level, logger, msg, ctx: {...}}.
     * Bez nowej zależności — własny logging.Formatter z json.dumps.
2. W main.py: setup_logging() w lifespan startup.
3. Grep `print(` w backend/ — każdy zamień na logger.info/warning/error z context dict.

NIE WOLNO:
- Logować ścieżek użytkownika na INFO (privacy) — tylko hash. PII tylko na ERROR.
- Logować embeddingów / nazw osób (PII).
- Dodawać python-json-logger jeśli nie jest już w requirements.txt.

ACCEPTANCE:
- pytest → tmpdir log file powstaje
- grep 'print(' main.py ai_core.py database.py = 0
- Manual: 5 sample log lines w PR description

DELIVERABLE: diff + przykładowe 5 linii logu w PR.
```

## Labels

`P0, phase-1, owner-grzesiek, backend, observability`

## Definition of Done

- [ ] Kod scalony do `main` przez PR
- [ ] PR ma minimum 1 approval od drugiej osoby
- [ ] CI zielone (lint + pytest + npm build, jeśli istnieje)
- [ ] Acceptance criteria spełnione i sprawdzone manualnie
- [ ] Dokumentacja zaktualizowana jeśli zmiana publicznego API
- [ ] CHANGELOG.md ma wpis w sekcji `[Unreleased]`

---
*Generated from `docs/PRODUCTION_ROADMAP.md` task `1.1.2`.*
