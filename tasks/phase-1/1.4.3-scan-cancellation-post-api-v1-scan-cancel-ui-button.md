# [1.4.3] Scan cancellation: POST /api/v1/scan-cancel + UI button

> **Owner:** Luq | **Hours:** 6 | **Priority:** P0 | **Phase:** 1 | **Source:** Cursor

## Context

Aktualnie skan jest non-cancellable. User chce 'Stop' w trakcie. ScanProgressState.cancelled flag + button w UI + polling.

## Files affected
- `main.py`
- `ai_core.py`
- `src/components/ScanProgress.tsx`
- `tests/integration/test_scan_cancel.py`

## Dependencies
- 1.4.1

## Acceptance Criteria

- [ ] POST /api/v1/scan-cancel ustawia ScanProgressState.cancelled = True
- [ ] ai_core scan loop sprawdza flagę MIĘDZY BATCHAMI (max latency = 1 batch)
- [ ] Po cancel: photos już processed=1 zostają, reszta processed=0 (resume możliwy)
- [ ] UI Stop button w ScanProgress (visible tylko gdy scanning=True)
- [ ] Confirm dialog przed cancel
- [ ] pytest: cancel po 2 batchach → processed > 0 i < total + state.cancelled=True
- [ ] Vitest: button click → POST
- [ ] Manual: skan 1000, cancel po 5s, restart aplikacji → resume działa

## Cursor Prompt

> Wklej 1:1 do Cursor Composer / Agent mode. Pamiętaj że `.cursorrules` w roocie repo jest aktywne.

```
ROLA: Senior Python (asyncio) + React engineer.

TASK: Scan cancellation end-to-end.

WYMAGANIA:
1. Backend:
   - ScanProgressState (już istnieje) — dodaj pole `cancelled: bool = False`.
   - POST /api/v1/scan-cancel: state.cancelled = True; return {ok: True}.
   - W ai_core.scan_directory pętla:
     for batch in batches:
       if state.cancelled: logger.info('scan cancelled'); break
       process(batch)
   - Po loop: finalize_partial() — commit DB, set state.state='cancelled'.
2. Frontend:
   - ScanProgress.tsx: gdy scanning=True → button 'Stop scan'.
   - onClick → confirm() → POST /api/v1/scan-cancel → polling /api/v1/progress aż state='cancelled'.
3. UWAGA:
   - Cancel mid-batch NIE — czekamy do końca batcha (DeepFace nie wspiera mid-cancel bezpiecznie).
   - Max latency cancellation = czas 1 batcha = ~5-10s przy batch=32.

ACCEPTANCE: 1 pytest backend + 1 vitest + manual flow.

DELIVERABLE: diff + 2 testy + manual screencast (opt).
```

## Labels

`P0, phase-1, owner-luq, backend, frontend, ux`

## Definition of Done

- [ ] Kod scalony do `main` przez PR
- [ ] PR ma minimum 1 approval od drugiej osoby
- [ ] CI zielone (lint + pytest + npm build, jeśli istnieje)
- [ ] Acceptance criteria spełnione i sprawdzone manualnie
- [ ] Dokumentacja zaktualizowana jeśli zmiana publicznego API
- [ ] CHANGELOG.md ma wpis w sekcji `[Unreleased]`

---
*Generated from `docs/PRODUCTION_ROADMAP.md` task `1.4.3`.*
