# [1.3.2] Schema versioning + numbered SQL migrations (no Alembic)

> **Owner:** Grzesiek | **Hours:** 8 | **Priority:** P0 | **Phase:** 1 | **Source:** Cursor

## Context

Decyzja Luqa: NIE używamy Alembic (overhead dla 1 SQLite). System własny: schema_migrations table + migrations/00X_*.sql + backup-before-migrate.

## Files affected
- `migrations/`
- `database.py`
- `tests/test_migrations.py`

## Dependencies
_None_

## Acceptance Criteria

- [ ] Tabela schema_migrations(version INTEGER PK, applied_at TIMESTAMP)
- [ ] apply_pending_migrations(conn): porównuje MAX(version) z plikami, exec w transakcji, INSERT version, COMMIT
- [ ] Failure → ROLLBACK + raise (NIE silent skip)
- [ ] Backup before migrate: organizer.db → organizer.db.bak.{version}
- [ ] Dry-run: PHOTO_ORGANIZER_MIGRATE_DRY_RUN=1 → print plan, no exec
- [ ] 3 pytest: creates_schema, dry_run, failure_rollback (z bak istniejącym po revert)

## Cursor Prompt

> Wklej 1:1 do Cursor Composer / Agent mode. Pamiętaj że `.cursorrules` w roocie repo jest aktywne.

```
ROLA: Senior Python/SQLite engineer.

TASK: System numbered migrations (BEZ Alembic).

WYMAGANIA:
1. Struktura:
   migrations/
     001_initial.sql      # CREATE TABLE z obecnego schema
     002_add_has_faces.sql
     003_indexes.sql      # z 1.3.1
2. CREATE TABLE schema_migrations(version INTEGER PRIMARY KEY, applied_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP).
3. apply_pending_migrations(conn):
   - current = SELECT COALESCE(MAX(version), 0) FROM schema_migrations
   - files = sorted(migrations/*.sql)
   - dla każdej > current:
     - BEGIN; exec sql; INSERT INTO schema_migrations (version) VALUES (N); COMMIT
     - Failure → ROLLBACK; raise
4. Dry-run: env PHOTO_ORGANIZER_MIGRATE_DRY_RUN=1 → tylko logger.info plan.
5. BACKUP BEFORE MIGRATE: kopia organizer.db → organizer.db.bak.{current_version} PRZED apply.
   Failure → restore z bak.

NIE WOLNO:
- ALTER TABLE w autocommit (musi w transakcji).
- Migracja drop column bez bak.
- Alembic (decyzja architektoniczna — Luq).

ACCEPTANCE: 3 testy + manual fresh DB → apply → 4 tables.

DELIVERABLE: folder migrations/ + diff database.py + 3 testy.
```

## Labels

`P0, phase-1, owner-grzesiek, backend, database`

## Definition of Done

- [ ] Kod scalony do `main` przez PR
- [ ] PR ma minimum 1 approval od drugiej osoby
- [ ] CI zielone (lint + pytest + npm build, jeśli istnieje)
- [ ] Acceptance criteria spełnione i sprawdzone manualnie
- [ ] Dokumentacja zaktualizowana jeśli zmiana publicznego API
- [ ] CHANGELOG.md ma wpis w sekcji `[Unreleased]`

---
*Generated from `docs/PRODUCTION_ROADMAP.md` task `1.3.2`.*
