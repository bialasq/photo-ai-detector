# [4.2.1] Sentry integration, DEFAULT OFF, opt-in w Settings

> **Owner:** Grzesiek | **Hours:** 6 | **Priority:** P1 | **Phase:** 4 | **Source:** PDF

## Context

Crash reporting. Offline-first USP — DEFAULT OFF. User opt-in w Settings → checkbox 'Help us improve by sending crash reports'.

## Files affected
- `sentry_config.py`
- `src/components/Settings.tsx`
- `docs/PRIVACY.md`

## Dependencies
_None_

## Acceptance Criteria

- [ ] Sentry DSN w env SENTRY_DSN (optional)
- [ ] User must explicit opt-in (checkbox unchecked default)
- [ ] Settings persisted w DB (settings table)
- [ ] PII scrub: paths → hashed, names → '<redacted>'
- [ ] docs/PRIVACY.md: co wysyłamy, kiedy, jak wyłączyć

## Cursor Prompt

> Wklej 1:1 do Cursor Composer / Agent mode. Pamiętaj że `.cursorrules` w roocie repo jest aktywne.

```
ROLA: Senior backend + privacy engineer.

TASK: Sentry opt-in crash reporting.

WYMAGANIA:
1. sentry_config.py:
   def init_sentry_if_enabled():
     if not get_user_setting('telemetry_enabled', False): return
     dsn = os.getenv('SENTRY_DSN')
     if not dsn: return
     sentry_sdk.init(dsn=dsn, before_send=scrub_pii, traces_sample_rate=0.0)
   
   def scrub_pii(event, hint):
     # remove paths, names, emails
     # replace with hashes or '<redacted>'
     return event
2. Settings UI: checkbox 'Send crash reports to help improve the app (opt-in)'.
3. Tooltip: link do docs/PRIVACY.md.
4. docs/PRIVACY.md: explicit list of fields sent.

NIE WOLNO:
- Default opt-in (USP offline-first).
- Send PII (paths, names, embeddings).
- Performance monitoring traces (tylko crash reports).

ACCEPTANCE: opt-in test → crash → event w Sentry; default off → no events.

DELIVERABLE: diff + docs/PRIVACY.md.
```

## Labels

`P1, phase-4, owner-grzesiek, backend, frontend, observability`

## Definition of Done

- [ ] Kod scalony do `main` przez PR
- [ ] PR ma minimum 1 approval od drugiej osoby
- [ ] CI zielone (lint + pytest + npm build, jeśli istnieje)
- [ ] Acceptance criteria spełnione i sprawdzone manualnie
- [ ] Dokumentacja zaktualizowana jeśli zmiana publicznego API
- [ ] CHANGELOG.md ma wpis w sekcji `[Unreleased]`

---
*Generated from `docs/PRODUCTION_ROADMAP.md` task `4.2.1`.*
