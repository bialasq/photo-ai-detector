# [4.1.2] Code signing MSI Authenticode [BLOCKER]

> **Owner:** Luq | **Hours:** 6 | **Priority:** P0 | **Phase:** 4 | **Source:** PDF

## Context

Wykorzystaj certyfikat z 0.2. signtool.exe sign MSI. BEZ TEGO Defender blokuje, AV false positive, userzy odbijają.

## Files affected
- `.github/workflows/release.yml`
- `docs/RELEASE.md`

## Dependencies
- 0.2
- 4.1.1

## Acceptance Criteria

- [ ] MSI podpisany Authenticode (Properties → Digital Signatures)
- [ ] Verify-Signature MSI → Valid
- [ ] signtool sign /fd SHA256 /tr timestamp /td SHA256 ...
- [ ] CI uses signing cert from GitHub Secrets (PFX base64) z password
- [ ] docs/RELEASE.md: pełen flow + rotacja certyfikatu

## Cursor Prompt

> Wklej 1:1 do Cursor Composer / Agent mode. Pamiętaj że `.cursorrules` w roocie repo jest aktywne.

```
ROLA: Senior Windows deployment engineer.

TASK: Authenticode signing MSI.

WYMAGANIA:
1. Cert z 0.2 (PFX file z password) → GitHub Secrets:
   - SIGNING_CERT_PFX_BASE64 (base64 encoded PFX)
   - SIGNING_CERT_PASSWORD
2. release.yml job 'sign-msi':
   - decode PFX z secret → temp file
   - signtool sign /fd SHA256 /tr http://timestamp.digicert.com /td SHA256 /f cert.pfx /p $env:SIGNING_CERT_PASSWORD MSI_path
   - signtool verify /pa /v MSI_path → assert pass
   - cleanup temp PFX
3. docs/RELEASE.md sekcja signing + cert rotation procedure.

NIE WOLNO:
- Commit PFX / cert.
- Skip timestamp (bez timestamp signature expires z certem).

ACCEPTANCE: MSI verify-signature OK + Defender install bez warning.

DELIVERABLE: diff workflow + docs.
```

## Labels

`P0, phase-4, owner-luq, release, security, blocker`

## Definition of Done

- [ ] Kod scalony do `main` przez PR
- [ ] PR ma minimum 1 approval od drugiej osoby
- [ ] CI zielone (lint + pytest + npm build, jeśli istnieje)
- [ ] Acceptance criteria spełnione i sprawdzone manualnie
- [ ] Dokumentacja zaktualizowana jeśli zmiana publicznego API
- [ ] CHANGELOG.md ma wpis w sekcji `[Unreleased]`

---
*Generated from `docs/PRODUCTION_ROADMAP.md` task `4.1.2`.*
