# [4.3.1] Backup feature: Help → Export library → ZIP

> **Owner:** Luq | **Hours:** 6 | **Priority:** P0 | **Phase:** 4 | **Source:** PDF

## Context

Export: organizer.db + faiss.index + cache miniaturek → ZIP. Restore w v1.1 (na razie tylko export).

## Files affected
- `backup.py`
- `src/components/BackupExport.tsx`
- `tests/integration/test_backup.py`

## Dependencies
_None_

## Acceptance Criteria

- [ ] POST /api/v1/backup/export → ZIP w Downloads (lub tauri save dialog)
- [ ] ZIP zawiera: organizer.db, faiss.index, faiss.index.ids.pkl, thumbnails/ (opt — flagą)
- [ ] 1 pytest: assert ZIP integrity
- [ ] UI button Help → Export library

## Cursor Prompt

> Wklej 1:1 do Cursor Composer / Agent mode. Pamiętaj że `.cursorrules` w roocie repo jest aktywne.

```
ROLA: Senior backend engineer.

TASK: Library backup ZIP.

WYMAGANIA:
1. backup.py:
   def export_library(output_path: Path, include_thumbnails: bool = False):
     with zipfile.ZipFile(output_path, 'w', zipfile.ZIP_DEFLATED) as zf:
       zf.write(get_db_path(), 'organizer.db')
       if faiss_index_exists(): zf.write(faiss_path, 'faiss.index')
       if include_thumbnails: add_dir_to_zip(zf, thumbnails_dir, 'thumbnails/')
2. POST /api/v1/backup/export {include_thumbnails: bool, output_path: str} (frontend daje path z Tauri save dialog).
3. Frontend Help → Export library → Tauri saveDialog → POST → toast 'Saved to {path}'.

NIE WOLNO: backup w trakcie scan (file lock). Sprawdź state.phase != 'scanning'.

ACCEPTANCE: ZIP integrity + 1 test.

DELIVERABLE: diff + test.
```

## Labels

`P0, phase-4, owner-luq, backend, frontend, feature`

## Definition of Done

- [ ] Kod scalony do `main` przez PR
- [ ] PR ma minimum 1 approval od drugiej osoby
- [ ] CI zielone (lint + pytest + npm build, jeśli istnieje)
- [ ] Acceptance criteria spełnione i sprawdzone manualnie
- [ ] Dokumentacja zaktualizowana jeśli zmiana publicznego API
- [ ] CHANGELOG.md ma wpis w sekcji `[Unreleased]`

---
*Generated from `docs/PRODUCTION_ROADMAP.md` task `4.3.1`.*
