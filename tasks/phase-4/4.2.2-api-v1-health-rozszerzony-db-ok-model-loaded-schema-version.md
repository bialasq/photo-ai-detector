# [4.2.2] /api/v1/health rozszerzony (db_ok, model_loaded, schema_version, disk_free)

> **Owner:** Grzesiek | **Hours:** 3 | **Priority:** P0 | **Phase:** 4 | **Source:** Cursor

## Context

Aktualny /health to ok=True. Rozszerz: db_ok, model_loaded, schema_version, disk_free_gb. Splash (4.1.5) i diagnostics (4.2.3) zależą od tego.

## Files affected
- `main.py`
- `tests/integration/test_health.py`

## Dependencies
_None_

## Acceptance Criteria

- [ ] Response: {ok: bool, db_ok: bool, model_loaded: bool, schema_version: int, disk_free_gb: float, uptime_seconds: int, version: str}
- [ ] Każde pole sprawdzane real-time
- [ ] 2 pytest (happy + degraded)

## Cursor Prompt

> Wklej 1:1 do Cursor Composer / Agent mode. Pamiętaj że `.cursorrules` w roocie repo jest aktywne.

```
ROLA: Senior backend engineer.

TASK: Health endpoint rozszerzony.

WYMAGANIA:
1. main.py GET /api/v1/health:
   - db_ok: PRAGMA quick_check fetch → 'ok'
   - model_loaded: DeepFace model w pamięci (cache flag)
   - schema_version: SELECT MAX(version) FROM schema_migrations
   - disk_free_gb: shutil.disk_usage(get_db_path().parent).free / 1e9
   - uptime_seconds: time.monotonic() - START_TIME
   - version: importlib.metadata.version or __version__
2. 2 testy: happy + sztuczna degradacja (mock disk_usage → 0)

ACCEPTANCE: response shape match + 2 testy.

DELIVERABLE: diff + testy.
```

## Labels

`P0, phase-4, owner-grzesiek, backend, observability`

## Definition of Done

- [ ] Kod scalony do `main` przez PR
- [ ] PR ma minimum 1 approval od drugiej osoby
- [ ] CI zielone (lint + pytest + npm build, jeśli istnieje)
- [ ] Acceptance criteria spełnione i sprawdzone manualnie
- [ ] Dokumentacja zaktualizowana jeśli zmiana publicznego API
- [ ] CHANGELOG.md ma wpis w sekcji `[Unreleased]`

---
*Generated from `docs/PRODUCTION_ROADMAP.md` task `4.2.2`.*
