# [4.1.5] Splash screen 'Loading AI models...' (cold start)

> **Owner:** Luq | **Hours:** 4 | **Priority:** P0 | **Phase:** 4 | **Source:** Cursor

## Context

DeepFace + TF cold start ~5-10s. Bez splash userzy myślą że apka zawiesiła się.

## Files affected
- `src/components/Splash.tsx`
- `src/App.tsx`
- `src-tauri/src/lib.rs`

## Dependencies
_None_

## Acceptance Criteria

- [ ] Splash widoczny przed /health responds 200 + model_loaded=true
- [ ] Animation (spinner) + tekst 'Loading AI models... (~10s)'
- [ ] Timeout 60s → error screen 'Failed to start, see logs'

## Cursor Prompt

> Wklej 1:1 do Cursor Composer / Agent mode. Pamiętaj że `.cursorrules` w roocie repo jest aktywne.

```
ROLA: Senior frontend + UX engineer.

TASK: Splash screen przy cold start.

WYMAGANIA:
1. src/components/Splash.tsx:
   - <img logo /> + <Spinner /> + 'Loading AI models... (~10s)'
   - Czeka aż fetch('/api/v1/health') zwraca {ok: true, model_loaded: true}
   - Polling co 500ms, max 60s
   - Timeout → error screen z linkiem do logs (Open in Explorer button)
2. App.tsx: render <Splash /> dopóki appState='loading'; potem main UI.

ACCEPTANCE: manual cold start → splash visible ~5-10s → main UI.

DELIVERABLE: diff + screencast (opt).
```

## Labels

`P0, phase-4, owner-luq, frontend, ux`

## Definition of Done

- [ ] Kod scalony do `main` przez PR
- [ ] PR ma minimum 1 approval od drugiej osoby
- [ ] CI zielone (lint + pytest + npm build, jeśli istnieje)
- [ ] Acceptance criteria spełnione i sprawdzone manualnie
- [ ] Dokumentacja zaktualizowana jeśli zmiana publicznego API
- [ ] CHANGELOG.md ma wpis w sekcji `[Unreleased]`

---
*Generated from `docs/PRODUCTION_ROADMAP.md` task `4.1.5`.*
